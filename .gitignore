# 1) 기본적으로 전부 무시
*

# 2) Git 관련/프로젝트 파일만 허용
!.gitignore
!/src/
!/public/
!/index.html
!/package.json
!/package-lock.json
!/tsconfig*.json
!/vite.config.ts
!/README.md
!/eslint.config.js

# 3) (선택) yarn/pnpm 쓰면 이것도 허용
!/yarn.lock
!/pnpm-lock.yaml

# 4) IDE/OS 파일 무시
.vscode/
.DS_Store
Thumbs.db

# 5) node_modules 무시 (위에서 기본적으로 *로 무시되지만 안전하게 한 번 더)
node_modules/
